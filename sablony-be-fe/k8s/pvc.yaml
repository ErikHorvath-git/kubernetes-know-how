apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: exam-pvc
  namespace: exam-horvath
spec:
  storageClassName: manual    # MUSÍ byť rovnaké ako v pv.yaml
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
