# PRVÁ ConfigMap — env premenné pre BE (PHP)
# Pattern: envFrom → všetky kľúče sú v PHP cez getenv()
apiVersion: v1
kind: ConfigMap
metadata:
  name: be-config
  namespace: exam-horvath
data:
  APP_NAME: "Exam HorvathVK"
  LOG_LEVEL: "INFO"
  MAX_UPLOAD_MB: "10"
