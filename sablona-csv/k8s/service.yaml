apiVersion: v1
kind: Service
metadata:
  name: exam-svc
  namespace: exam-horvath
spec:
  selector:
    app: exam-app
  type: NodePort
  ports:    
    - port: 80         
      targetPort: 80   
      nodePort: 30080  
